"""Evaluation harness for the support agent.

The golden set, the trajectory + answer scorers, an LLM-as-judge for the cases a
substring cannot grade, and the runner that gates on all of it. Import the pieces
from their modules; this package marker keeps them together.
"""
